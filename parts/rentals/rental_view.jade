template(name='rental_view')
    with current_doc
        .ui.stackable.padded.grid
            .row
                .four.wide.column 
                    a.ui.basic.button(href="/rentals" title='rentals')
                        i.left.chevron.icon
                        +i name='clock' classes='ui avatar image'
                    .ui.header view rental
                    if can_edit
                        a.ui.big.icon.basic.button(href="/rental/#{_id}/edit")
                            i.pencil.icon
                    +image_view key='image_id' label='image' direct=true
                    //- +voting_full
                    //- +viewing
                .eight.wide.column
                    h1.ui.header #{title} 
                    +textarea_view key='description' icon='content' label='description' direct=true
                    +array_view key='location_tags' label='location tags' icon='marker' direct=true
                    +array_view key='tags' label='tags' icon='tags' direct=true
                    +single_user_view key='resident' label='resident' direct=true
                    +text_view key='contact_info' label='contact info' icon='info' direct=true
                    div
                    +rental_reservations
                .four.wide.column 
                    .ui.basic.green.button.new_reservation
                        |reserve
                
                    +number_view key='hourly_rate' label='hourly rate' icon='money' direct=true
                    +number_view key='daily_rate' label='daily rate' icon='calendar' direct=true
                    div
                    +rental_history
                    +comments

                
template(name='rental_history')
    +ismall name='rss'
    .ui.inline.header
        |activity
    .ui.list
        each rental_events
            .item #{text} #{when}
            
template(name='rental_subscription')
    //- +ismall name='renew-subscription'
    //- .ui.inline.header
    //-     |subscription settings
    .ui.button.subscribe subscribe to updates
    .ui.list
        each subscribers
            .item #{text} #{when}
            
template(name='rental_reservations')
    .ui.inline.header
        +i name='invoice'
        |reservations
    each reservations
        .ui.segment
            .ui.inline.header #{when}
            .ui.inline.small.header by #{_author_username}
            a.ui.basic.icon.button(href="/reservation/#{_id}/view")
                i.right.chevron.icon
